# These are required for developing the package (running the tests, building
# the documentation) but not necessarily required for _using_ it.
codecov
coverage
flake8
pytest
# There are dependencies for the research
xpdacq
area-detector-handlers
pdfstream
